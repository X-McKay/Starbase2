# Current stopped-deployment readiness audit

Read-only audit: 2026-09-09 approximately03:15 UTC (2026-09-08 local), Kubernetes context `default`. Local `/Users/al/git/kubani` and GitHub main both resolve to `d72107f4e55d422313dfd638c54f40f5c1f35ea2`. No applicable `AGENTS.md` was found in that checkout. No Kubernetes, GitOps, database, registry or credential mutation was performed; no credential contents were read.

## Observed current state

- The inactive Starbase2 preparation from Kubani PR127 is historical. Commit `cd565b6ed4ffbe860c82ab0900b2628bfc67e867` deliberately removed the Starbase2 directory and its unreferenced Flux manifest along with predecessor retirement work. Current main contains neither `infrastructure/gitops/apps/starbase2/` nor a Starbase2 Flux Kustomization. Reintroducing a stopped installation requires a fresh reviewed change against current main, not resurrection of an assumed active owner.
- `kubectl --context default get namespaces starbase2-prod database temporal` returned NotFound for `starbase2-prod`; `database` and `temporal` are Active. Flux `apps`, `databases`, `infrastructure`, and `flux-system` report Ready. There is no Starbase2 reconciliation object.
- PostgreSQL is Running2/2 on `strix`. Temporal frontend/history/matching/web/worker are Running1/1 on `rig0`. Nodes `asio`, `rig0`, and `strix` are amd64 with no taints. `sparky` is arm64 and has the GPU NoSchedule taint. This is observed pod readiness/placement, not an authenticated database/Temporal admission or restore test.
- Neither destination namespace currently lists a Starbase2 dependency ingress policy. Existing default-deny ingress and platform allowances remain in place. Fresh scoped database-side and Temporal-side allowances are needed.
- The old local preview lives at `.local/kubani-preflight/gitops-preview/apps/starbase2/`, with its separate Flux preview in `.local/kubani-preflight/gitops-preview/starbase2-kustomization.json`. Its configuration is an old unpublished arm64 candidate. `.local/deployment-preview/config.json` is also intentionally invalid preview data. `.local/deploy/production.json` does not currently exist. None is an executable current production release.
- `evidence/linux-release-amd64/publication-plan.json` records published/verified images of revision `71ca83dd7e5a9d763760a81f900e72aaaae2ea15`. Those images predate current installation/capability/source-time changes. Anonymous registry HEAD returned401; current registry digest existence was not reverified with credentials. The three owned SQL schema files have no diff from that qualified revision, but this does not qualify the current implementation or authorize a migration.

## Concrete staged next work

1. Prepare a fresh release configuration at `.local/deploy/production.json` with explicit context `default`, installation/namespace/Temporal namespace `starbase2-prod`, database `starbase2_prod`, queue `starbase2-prod-v1`, platform `linux/amd64`, current service addresses, `replicas:0`, and `accept_work:false`. Do not fill it with invented digests or stale candidate labels. Namespace/SA/policy design and owner review can proceed independently while fresh image qualification is prepared.
2. Freeze the current backend source through its authorized review/commit workflow; build and qualify fresh native amd64 Core/worker images, including the latest metadata contract. Publish only through the separately authorized artifact workflow. Bind exact committed revision and registry digests into the configuration. Old images must not be presented as qualification of the current frontend/backend behavior.
3. Run `mise exec -- just deploy render --config .local/deploy/production.json`, then inspect `kubectl kustomize .local/deploy/bundle`, release checksums, zero replicas, disabled admission, Secret references, namespace ownership and dependency selectors. Keep `migration.json` outside automatic reconciliation. Run offline deployment guards and read-only preflight. A missing application namespace on the first preflight is expected and must be reported; it is not permission to adopt another namespace.
4. Prepare a fresh Kubani diff under `infrastructure/gitops/apps/starbase2/`, with one dedicated Flux owner in `infrastructure/gitops/flux-system/` and its root reference. Depend on current `databases` and `apps` readiness. Reconcile through Kubani's reviewed workflow, keeping replicas0/admissionfalse. Verify namespace, service account, and narrowly scoped app/database/Temporal policies. Do not add the same objects to both the apps aggregate and a dedicated owner.
5. With already authorized operator-managed credentials (never extracted into evidence), verify dependency connectivity and owned fresh database/roles/Temporal namespace. Execute separately reviewed provision/secrets/migrate/grants steps with the application stopped; retain migration failure evidence and ownership checks. Verify the schema and runtime-role limits before starting any worker. Database role existence, Temporal namespace existence and current operator libpq setup were not inspected by this read-only audit.
6. Prepare an explicitly disposable read-only pilot before durable admission. The current renderer disables field/memory/inference and allows egress only to DNS/PostgreSQL/Temporal. A real GitHub pilot therefore requires an explicit field-only capability and exact-target credential/network design; the local GET-only adapter does not itself establish cluster egress or credential isolation. Keep inference/memory/repair/external writes disabled. Bound duration, repository, provider requests, duty frequency, evidence retention and cleanup; test cancellation/restarts/client-closed operation. Label all pilot records disposable and exclude them from production migration.
7. Before accepting durable work, satisfy the independent PostgreSQL backup key and proven restore/RPO/RTO gate, plus identity and recovery requirements of that admission scope. Do not impose that durable-data gate on harmless stopped resource preparation; do not use a disposable pilot to claim it has passed.

This record is a preparation audit, not activation approval or deployed readiness. No stopped bundle was rendered or applied during this read-only task.

## Shared-service ingress isolation check

A subsequent read-only check of context `default` verified the relevant live pod labels and all ingress NetworkPolicies in `database` and `temporal`. PostgreSQL pod `postgresql-0` has `app.kubernetes.io/name=postgresql`, matching the candidate database policy. Temporal pod `temporal-frontend-6cf8bd9769-kjtpz` has `app.kubernetes.io/name=temporal`, `app.kubernetes.io/instance=temporal`, and `app.kubernetes.io/component=frontend`, matching all candidate frontend selector terms.

Both namespaces already have `default-deny-ingress` with `podSelector:{}` and `policyTypes` including Ingress, so both destination pods are already ingress-isolated. Both also have `allow-same-namespace` selecting all pods and allowing same-namespace peers, plus `allow-traefik-ingress` selecting all pods and permitting the selected kube-system Traefik source. PostgreSQL additionally has `allow-auth-postgresql` and `allow-temporal-postgresql`, each selecting `app.kubernetes.io/name=postgresql` and allowing TCP5432 from the named source namespace.

Therefore these proposed Starbase2 ingress policies would not be the first policies selecting either shared service and would not remove the existing additive allowances. They add only the candidate's exact application namespace/pod-label source and TCP5432 or TCP7233. The five-resource candidate remains unchanged and unapplied. This verifies the first-policy isolation concern against the observed baseline, not CNI enforcement, authenticated connectivity or future policy drift; recheck the live baseline before installation.
