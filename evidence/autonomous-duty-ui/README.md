# Autonomous duty UI acceptance

Reproduced the missing explicit same-row history refresh control in `history-refresh-before.log` (exit1). Added Refresh selected plus row activation, preserving selected ID, fetched evidence, and offline/fixture guards. Focused history regression tests pass.

Command shows all configured field duties, including Watchkeeper and missing-build targets, with observed enabled/paused state, inference request, observation interval, target cooldown and daily limit. Pause/Resume preserves inference and submits the next generation without optimistic readback changes. V4 duty lost-response reconciliation requires exact retained settings, including inference and generation; its HTTP fixture passes with seven intended POSTs and GET reconciliation.

Observation rows use concise AI status/reason. Detail renders unverified model advice summary/recommendation plus model/usage/admission facts; complete JSON remains behind the existing full-record toggle. Unavailable, skipped daily admission/cooldown, failed observations and not-requested advice remain distinct. All four focused Godot scripts pass; registrations are in `scripts/check_world.py`.

The existing read-only `live_capture.gd` now captures retained Watchkeeper and reviewer reasoning detail when present. Explicit `--require-reasoning-evidence` requires both roles to have completed retained unverified advice with real source. It launches no provider call and submits no command. Use an actual native/exported application against the separately qualified loopback backend; root coordinates source freeze, real provider evidence and final export. Inspect screenshots for readable advice and configured duty state at native and compact sizes. Engine-native capture avoids the current ambiguous/stale CUA app identity; it does not claim manual OS input was verified.

Expanded Godot-only scope: configured field duties now have native creation/edit controls with explicit inference, target policy, retained generation checks and unavailable-target refusal. Static configured GitHub targets no longer require a dynamic repository watch. Manual field observations expose an explicit advice opt-in. Journal navigation is handled internally by the parent-owned HUD changes. Native duty editor, responsive board, overview, and seven-POST isolated command transport checks passed without logged errors.

The opt-in `--qualify-duty-controls` live capture flag authorizes only `starbase2-watchkeeper-autonomous` and `starbase2-github-autonomous`. It requires both initially enabled with inference and a completed autonomous timer run, emits actual native Pause/Resume button signals, waits for exact authoritative settings and successive generations plus settled transport, and records every action before proceeding. Uncertain outcomes stop without retry and require reconciliation. Confirmed states have native screenshots. A synthetic harness check passed; this does not certify live controls or real model advice.

Full world `world-01.log` passed before the expanded Godot-only scope. Export `20260910-final-01` failed its initial native walk: its screenshot proves an opened Connection panel and modified address (`mhttp...`) during concurrent external input; panel state blocks walking. That failed attempt is retained as input-contaminated. Export `final-02` was stopped (exit130) when the user expanded the native-only scope. Neither directory is a qualified artifact. A fresh full world check and export are required for the expanded source.

Expanded integrated validation: `world-02.log` passed all checks through structures, then failed the old room camera/route test. Diagnostic `rooms-diagnostic-01.log` captured a stale camera focus at the exterior position while the test expected the room focus; `process_frame` wakes before `Node._process`. The corrected test waits through a completed process update, honors authored CameraFocus, and retains the same projected click, collision and physical-arrival assertions. `rooms-after.log` passed clean, followed by `interaction-final.log`. Prior successful stages were not repeated. Final remaining HTTP results are retained in `commands-final.log` and `operations-final.log`. Native source is commit `6189873`; the outage capture precondition separately samples retained IDs immediately before stopping reads so legitimate autonomous arrivals earlier in capture do not cause a false loss-of-state assertion.

Native `final-03` passed exported smoke and the first native walk, but Habitat arrival failed with `manual_input_seen=true`, empty route, no collisions or open panel. Its diagnostic records and artifact remain preserved. The old launcher repeatedly activated the app every two seconds; normal physical keyboard polling then canceled automated routes. Capture-only input isolation now disables external GUI/unhandled events and manual movement polling while preserving real route physics and explicit harness button/E calls. Ordinary launch stays unchanged. The launcher now uses background Launch Services (`open -g`) without activation. Focused `capture-input-final.log` passed normal-input, physical traversal, Journal-button and console-E assertions; `export-background-tests.log` passed background launch and exact-owned-process cleanup. Initial regression used an obsolete Journal signal and was corrected to emit the current actual button (`capture-input.log` retains that failure). Final import passed. Qualification records explicitly identify automated input isolation and do not claim manual OS input coverage. Fresh final-04 is required.

Background `final-04` timed out after 180 seconds before the first native Godot output or pixel capture; its exported smoke passed. The exact owned process was cleaned up. The runner now uses one normal Launch Services launch (no `-g` and no periodic activation); capture input isolation is unchanged. Updated runner tests and existing export tests pass (11 total in `export-single-launch-tests.log`). This is a launch-precondition correction after retained evidence, not a quiet-input retry. Fresh final-05 must establish actual rendered pixels and remaining acceptance.

Final result: `20260910-final-05` qualified successfully (exit0) with one normal Launch Services launch, no extra activation, actual rendered pixels, all exported journeys and read-only live reasoning. Exact source tree SHA-256 is `20d864f5c60969d912d83aeaa05506e57cd1d78797ff0609e12e0b659b075521`; ZIP is `c1362ec4f94d953adaf12e25b2a6e93278ff5c2019b350c044c158e5fc6057c5`. The qualification manifest and selected native pixels are copied to `native-final/`; the full local ZIP/capture set remains under `.local/reviews/autonomous-duty-ui/20260910-final-05`.

The exact ZIP was separately extracted and its executable/pack hashes verified before `--require-reasoning-evidence --qualify-duty-controls` acceptance. Both named duties passed actual native button Pause → authoritative generation1 disabled → Resume → authoritative generation2 enabled. Watchkeeper retained inference=true and 300 seconds; reviewer retained inference=true and 900 seconds. Eight submitted/confirmed events, no uncertainty and no retries, are in `native-final/live-duty-controls.json`; matching artifact provenance, native logs and selected screenshots are retained beside it. This used the real approved backend after both autonomous timers had completed. It did not start additional manual observations or provider calls. Pausing/resuming changes future duty timers, as explicitly authorized.

Both retained reasoning views show source timestamps and readable UNVERIFIED model advice. Compact connection controls and the scrollable duty editor are legible. Crew labels remain crowded behind the Command workspace; this is a recorded visual limitation for the living-world follow-up. Engine-native button and keyboard-action paths were verified; manual OS input was deliberately isolated during capture and is not claimed. Parent owns deployment qualification, backend readback, evidence publication and the proposed inhabited-world follow-up.
