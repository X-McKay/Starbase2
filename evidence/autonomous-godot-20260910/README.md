# Autonomous duties through Godot · 2026-09-10

Status: exact images qualified and deployed; both real reasoning checks passed
functionally, duties are enabled and both actual timers have produced completed
observations. The final unsigned native export and explicit live Pause/Resume
acceptance passed; owner visual acceptance remains separate.

The approved scope enables recurring read-only observations for the configured
Starbase2 Kubernetes and GitHub targets, with optional hosted LLM advice. The
user subsequently selected Godot as the complete operator experience. Browser
dashboard routes are retired; the service root retains native session bootstrap
and connection guidance. Versioned APIs and independent infrastructure recovery
remain available. Persistent reviewed memory follows this acceptance, not before.

Backend source `974800a` adds persisted daily admissions and cooldown, opt-in
duty inference, truthful advisory state, and additive contracts. Source `2b64a73`
retires browser dashboard delivery without changing native authentication.
Both failed and uncertain model attempts consume reservations. Observations
continue when reasoning is skipped. The configured policy is 24 admissions per
target per UTC day, separated by at least 3600 seconds. Planned observation
intervals are 300 seconds for Watchkeeper and 900 seconds for GitHub.

[Kubani PR149](https://github.com/X-McKay/kubani/pull/149), merge `4fb8889`, passed
all three CI checks and applied through Flux. [Image qualification](qualification-report.json)
passed all 17 PostgreSQL/Temporal checks. [Publication](publication.json) records
pull-verified immutable images; [live pod](live-pod.json) verifies the deployed
pair. [Deployed HTTP checks](deployed-service-page.json) confirm the dashboard
is absent. [Activation readback](activation-readback.json) records both enabled
duties and the two completed real-target reasoning requests.
[Actual timer evidence](first-both-timers.json) records the first GitHub run and
three Watchkeeper runs without manual tick dispatch. All completed with fresh
non-synthetic source; reasoning was skipped with zero calls because the hourly
cooldown remained active. Both schedules remained enabled at generation 0.

Local checks: 43 Rust tests, 127 Python tests, Clippy/Ruff/type checks, contract
regeneration and documentation checks passed. The native world suite and both
HTTP command fixtures passed for `974800a`; subsequent Godot-only controls passed fresh native qualification at source
`ce05721`, separately from the deployed backend source `2b64a73`. The real isolated Core service-page check proved
native session bootstrap and origin enforcement after dashboard retirement.

One synthetic real-endpoint field-advice call returned structured, unverified
advice using 816 input and 223 output tokens. Some suggested command syntax was
questionable: this is functional transport evidence, not reasoning-quality
evidence. Live configured target runs are a separate acceptance step.

The first native export qualification (`20260910-final-01`) was contaminated by
concurrent UI input. Its colony capture shows an unexpected Connection panel and
typed endpoint text, which stopped scripted movement. Failure evidence is retained
under the local review directory. No source defect was inferred from this run.
The superseded second attempt was stopped before qualification; a fresh source
and artifact must be qualified without concurrent UI interaction.
The third attempt passed colony walking and most packaged journeys but detected
physical keyboard input during the Habitat approach. Automated capture was
isolated from desktop input, and repeated foreground activation removed; this
changes explicit capture mode only. The fourth attempt stalled before Godot
stdout during background launch and hit its bounded timeout. The fifth uses one
normal initial Launch Services launch, without repeated activation. Manual OS input coverage is not claimed.

Completion requires qualified immutable images, healthy Flux rollout, retained
real target reasoning, actual server timer dispatch for both enabled duties,
and fresh Godot evidence including native control/readback and reconnect. No
durable production recovery, overnight reliability, PR review quality, or memory
quality claim is made by this bounded slice.

The two first real-target reasoning runs completed with retained source and one
model request each. Watchkeeper used 797 input/201 output tokens; GitHub used
575 input/230 output tokens. Costs were not reported. GitHub had no open PRs.
Advisory inspection found overinterpretation: Watchkeeper suggested comparing
Kubernetes resource versions to release timing, and GitHub suggested possible
configuration/access issues despite a complete empty PR listing. These are
unverified recommendations, not deterministic findings or authorized actions.
A future reasoning-quality candidate must address these cases in paired tests
before any claim of improved diagnosis or action authority.

Independent drain now covers V4 duties and repository watches as well as older
work. It requires an exact installation identity before mutations and accepts
an explicit canonical loopback Core address, so the pilot's 18787 endpoint
cannot be confused with another local Core on 8787. This operator-side change
was tested without invoking it on the enabled pilot. The final Python suite
passed 127 tests; a superseded timeout mock was updated to the bounded
launcher mechanism while retaining exact-child cleanup assertions.

The [living autonomy proposal](../../docs/living-autonomy.md) answers the owner's
next experience direction. Real field runs already drive crew movement/work
presentation, but idle actors return near their starting positions. Routing them
to the existing Habitat/commons for rest and social activity remains proposed,
with authored actor anchors and cross-building presentation still to implement.

## Final native acceptance

The final fifth export qualified with 41 capture/artifact entries, native
physical journeys, retained live reasoning, and client disconnect/reconnect.
The same exact ZIP/executable/pack was verified before a separate explicit
control run. Native Pause/Resume button handlers sent four writes, each confirmed
against Core: both duties moved from generation 0 to paused 1 to enabled 2,
preserving target, interval and inference. [Final readback](final-readback.json)
independently confirms both enabled. No additional manual model call was made.

See [native evidence](../autonomous-duty-ui/README.md) for the exact artifact,
manifest, selected captures and retained failed-attempt diagnoses. The ZIP
archive hash is `c1362ec4f94d953adaf12e25b2a6e93278ff5c2019b350c044c158e5fc6057c5`.
Explicit capture mode isolates physical desktop input; programmatic native
button signals and scripted physical journeys are qualified, not manual OS
keyboard/mouse use. Root inspected Habitat, duty editor, live reasoning, compact
connection and restored-duty screenshots. Text is readable; crew evidence labels
crowd one another behind the board, an owned visual follow-up in the living-world
proposal. This is an unsigned local macOS review build, not a distributed client
release. Persistent memory remains disabled.
