# Shift Change environment and capture evidence

`habitat-composition.png` is an intermediate native art view after central sofa,
warm flooring and lighting changes. Its accompanying intermediate logs retain
an animation API mismatch during parallel implementation; it is not a passing
integrated qualification. Full repeated logs remain in `.local/`.

`offline-capture-fence.log` records expected refusal (exit1) when the native
Shift Change capture flag is used without an offline fixture. No output folder
was created. The harness in `apps/world/shift_change_capture.gd` additionally
requires the board fixture and input isolation, stages initial positions once,
then uses actual physics, authored anchors and rig animation clips. Captures and
reports explicitly identify the synthetic fixture and a175-second wall-clock
limit plus179-second watchdog.

Run separate domestic and journey passes through the world capture hook after
all asset and animation source is frozen. Domestic records physical seating,
stand-up frames and immediate fast-job evidence. Journey additionally records
workstation travel/arrival, report-ready, physical return and offline/reconnect.
The native outcome and visual critique belong with those fresh reports rather
than being inferred from this intermediate composition image.
