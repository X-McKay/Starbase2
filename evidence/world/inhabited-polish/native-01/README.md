# Representative native inhabited journey

Launched pinned Godot 4.7.2 through macOS Launch Services with fixed 60 Hz and a 180-second bound. Exit 0; stderr empty; `inhabited-journey.json` contains no failures and 1,255 sampled moving frames. `source-hashes.json` records inputs at launch. Actual world processing, camera and HUD remained enabled throughout.

Reviewed `habitat-interior.png`, `habitat-guide.png`, `water-moving.png` and movement frames. Habitat now has readable dining/lounge zones, bedside lights/dividers and table settings, with central access to the console. Guide text fits the dock and stays descriptive. No evident furniture intersection or camera obstruction in these views. The spring has a dark center and turquoise shallow shelf. These stills alone do not prove water animation quality; the water agent's phase-controlled native captures complement the temporal assertions here. Default outdoor zoom makes the player small; close character isolation captures are needed to inspect subtle hair motion. No integrated foot-grounding defect was evident at this scale, but these frames cannot quantify the inherited model floor offset.

The new focused journey passed headless on its first run (`../journey-focused-01.log`). A subsequent existing furniture-contact test failed twice for Habitat (`../furniture-focused-01.log`); temporary instrumentation (`../furniture-diagnostic-01.log`) establishes that both rear dining-chair test starts are inside the new dining-table collider. Other Habitat furniture probes stop correctly. This is an invalid probe precondition; test correction is being coordinated, and the initial failures remain retained.

Follow-up: `test_remaining_structures.gd` now checks all four cardinal approaches, requires the capsule approach corridor to be clear of other furniture and the start to lie within room bounds, and fails if no valid probe exists. It retains the same attempted crossing and >0.26 m physical stopping clearance. Front/back-only intermediate validation exposed two Command props requiring lateral probes (`../furniture-focused-02.log`, retained). The completed four-direction test passes cleanly for all four structures (`../furniture-focused-03.log`). No runtime correction was required. Native captures predate this test-only source change.
