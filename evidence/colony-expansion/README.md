# Faster exploration and plateau expansion

Status: proposed

Final checks passed: full world and native command suites, documentation links,
Python lint and whitespace. Native controls reviewed at both viewport sizes.

The [baseline](before.log) reproduced the missing zoom interface. The
[focused check](focused.log) verifies faster route travel, both zoom buttons in
outdoor/interior/map views, reachable expansion land and no command dispatch.
The [world suite](world-verified.log) checks navigation, coast physics, the mainland seam,
continuous room traversal and native command boundaries.

The [initial suite](world.log) timed out because an old navigation assertion
classified x=45 as outside the shelf. That point is now inside the expanded
outline. The [next run](world-final.log) exposed one more obsolete cliff probe in the
colony landmark test. Boundary probes were moved to the new edge, including physical coast
contact checks and headland destinations; wall checks remain intact.

Native review: [expanded plateau](plateau.png) at 1280×800 and
[compact controls](compact.png) at 960×720. Buttons remain visible and readable.
Existing scene geometry and 6 m/s motion drive the unchanged skeletal walk cycle;
this does not add a separately authored sprint animation.

Plateau width/depth scale factors are 1.4 and 1.35; polygon area scales by 1.89.
Buildings remain in place. Northern decorative shoreline anchors expand with the
shared playable boundary. No backend or Meshy API changes or credit spending.
The earlier packaged build is not regenerated by this check.
